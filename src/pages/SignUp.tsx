import { AuthCardLayout, SignUpForm } from "../features/auth/components";

export default function SignUp() {
  return (
    <AuthCardLayout title="Sign up">
      <SignUpForm />
    </AuthCardLayout>
  );
}